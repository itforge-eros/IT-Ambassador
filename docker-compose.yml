version: "2"

services:

  caddy:
    image: stevepacker/caddy-php7
    container_name: ambassador
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "./Caddyfile:/etc/Caddyfile"
      - "./public:/srv"
      - "$HOME/.caddy:/root/.caddy"
